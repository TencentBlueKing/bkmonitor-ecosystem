[project]
name = "helloworld"
version = "0.1.0"
description = "蓝鲸观测平台的观测数据接入 demo (Python)"
readme = "README.md"
requires-python = ">=3.8"

[tool.black]
line-length = 120

[tool.isort]
profile = "black" # 兼容 black
line_length = 120
